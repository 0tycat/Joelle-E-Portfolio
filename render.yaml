services:
  - type: web
    name: joelle-e-portfolio-backend
    runtime: docker
    plan: free
    dockerfilePath: backend/composite_services/Dockerfile
    dockerContext: backend
    env: docker
    # Environment variables must be set in Render Dashboard
    # Go to: Settings → Environment
    # Required variables:
    #   SUPABASE_URL
    #   SUPABASE_SERVICE_KEY
    #   SUPABASE_ANON_KEY
