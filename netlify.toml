[build]
  base = "frontend"
  command = "npm install && npm run build"
  publish = "dist"

# SPA fallback so client-side routes work on Netlify
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
